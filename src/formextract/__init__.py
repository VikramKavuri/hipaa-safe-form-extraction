"""formextract — privacy-first, fully-local document AI.

Structured field extraction from handwritten compliance forms using a
vision-language model (Qwen2.5-VL via Ollama) combined with a classical
computer-vision preprocessing and checkbox-localization pipeline.

All inference runs locally; no document data leaves the machine.
"""

__version__ = "0.1.0"
